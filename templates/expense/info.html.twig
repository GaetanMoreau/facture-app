{% extends 'base.html.twig' %}

{% block title %}Dépense n°{{ expense.id }} - Devis&Facture {% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Dépense n°{{ expense.id }}</h1>
        <td>{{ expense.getName() }}</td>
        <td>{{ expense.getAmount() }}</td>
        <td>{{ expense.getStatus() }}</td>
    </div>
{% endblock %}
