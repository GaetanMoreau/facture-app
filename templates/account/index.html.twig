{% extends 'base.html.twig' %}

{% block title %}Mon profil
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>Vue d'ensemble</h1>
		<a class="btn" href="{{ path("app_client") }}">Liste des client</a>

	</div>
	<div class="example-wrapper">
		<h1>Bonjour</h1>
		{# Accéder à mon profil utilisateur #}
		{# <a href="{{ path('app_profile') }}">Profile</a>
									<a href="{{ path('app_logout') }}">Se déconnecter</a> #}
		{# Créer une condition pour afficher se connecer ou se deconnecter #}
			{% if app.user %}
				<a href="{{ path('app_logout') }}"> Se déconnecter</a>
		{% else %}
			<a href="{{ path('app_login') }}">Se connecter</a>
		{% endif %}
		{# <a href="{{ path('app_profile') }}">Profile</a> #}
	</div>
{% endblock %}
