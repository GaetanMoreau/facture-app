{% extends 'base.html.twig' %}

{% block title %}Mon profil
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>Vue d'ensemble</h1>
		<a class="btn" href="{{ path("app_client") }}">Liste des client</a>
		<a href={{ path('app_invoice') }}>Factures</a>

	</div>
	<div class="example-wrapper">
		<h1>Bonjour</h1>
		{% if app.user %}
			<a href="{{ path('app_logout') }}">
				Se déconnecter</a>
			<a href="{{ path('app_profile') }}">Profil</a>
		{% else %}
			<a href="{{ path('app_login') }}">Se connecter</a>
		{% endif %}
	</div>
{% endblock %}
